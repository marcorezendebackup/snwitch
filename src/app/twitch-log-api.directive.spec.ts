import { TwitchLogApiDirective } from './twitch-log-api.directive';

describe('TwitchLogApiDirective', () => {
  it('should create an instance', () => {
    const directive = new TwitchLogApiDirective();
    expect(directive).toBeTruthy();
  });
});
